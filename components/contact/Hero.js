import HeroPage, { HeroTitle } from '../HeroPage';

const HeroContact = () => {
  return (
    <HeroPage>
      <HeroTitle>
        Contactez-nous <span className="text-primary">maintenant</span>.
      </HeroTitle>
    </HeroPage>
  );
};

export default HeroContact;
