import HeroPage, { HeroTitle } from '../HeroPage';

const HeroBooking = () => {
  return (
    <HeroPage>
      <HeroTitle>
        Réservez votre table <span className="text-primary">maintenant</span> pour une expérience inoubliable.
      </HeroTitle>
    </HeroPage>
  );
};

export default HeroBooking;
