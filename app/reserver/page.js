import Layout from '@/components/Layout';
import HeroBooking from '@/components/reserver/Hero';
import ReservationForm from '@/components/reserver/ReservationForm';

const Company = () => {
  return (
    <Layout>
      <HeroBooking />
      <ReservationForm />
    </Layout>
  );
};

export default Company;
